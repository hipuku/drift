node_modules
dist
build
.DS_Store
*.local

# Env / secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*

# Playwright
# Unanchored: the end-to-end suite runs from client/, so `/test-results` would
# only have matched a root-level run that never happens.
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Local data
redis-data

# Redis local persistence
appendonlydir/
dump.rdb
*.rdb
